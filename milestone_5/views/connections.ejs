<!DOCTYPE html>
<html lang="en">

<head>
  <title>UNCC HTML</title>
  <meta charset="utf-8" />
  <link rel="stylesheet" type="text/css" href="../assets/style.css">
</head>

<body>
  <%- include ('partials/header.ejs') %>
  <%- include ('partials/upnav.ejs') %>


  <div class="newmiddle2">


    <% if (dataNew.length>0){ %>
    <% for (let i=0;i<dataNew.length;i++) { %>


    <div class="newmiddle2"><%= dataNew[i].key %></div>
    <% if(dataNew[i]['list']&&dataNew[i]['list'].length){ %>
    <% for (let j=0;j<dataNew[i]['list'].length;j++) { %>
    <ul>
      <li><a class="align" href='/connection/<%=dataNew[i]['list'][j].ID%>'><%= dataNew[i]['list'][j].diflev %></a>
      </li>
    </ul>
    <% } %>
    <% } %>
    <% } %>
    <% } %>




  </div>
  <%- include ('partials/footer.ejs') %>

</body>

</html>